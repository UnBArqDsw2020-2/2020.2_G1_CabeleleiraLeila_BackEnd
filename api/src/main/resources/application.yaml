spring:
    datasource:
        driverClassName: org.h2.Driver
        password: 
        url: jdbc:h2:mem:testdb
        username: sa
    jpa:
        database-platform: org.hibernate.dialect.H2Dialect
        hibernate:
          ddl-auto:  create-drop
    h2:
        console:
          enabled: true
          settings:
            web-allow-others: true
server:
  servlet:
    context-path: /api

jwt:
  secret: 84ab455e552048c99f4997a97e6576a4